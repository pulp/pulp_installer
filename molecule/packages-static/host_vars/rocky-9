---
# Needed because otherwise:
#     - nothing provides selinux-policy >= 34.1.38-1.el9 needed by pulpcore-selinux-1.3.2-1.el9.x86_64
pulp_install_selinux_policies: False

# These variables are used by molecule verify, not the installer itself
pulp_python_interpreter: /usr/bin/python3.9
